{
  "name": "pnpm-monorepo",
  "version": "1.0.0",
  "description": "Test pnpm monorepo",
  "scripts": {
    "build": "turbo run build",
    "clean": "rimraf packages/**/dist packages/**/node_modules packages/**/.turbo node_modules"
  },
  "license": "BSD-3-Clause",
  "packageManager": "pnpm@10.6.5",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.5.0"
  }
}
